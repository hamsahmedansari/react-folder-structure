import React from 'react';
import { PrivateRouteProps } from './PrivateRouteType';

const PrivateRoute:React.FC<PrivateRouteProps> = ({}) => {
    return (
        <p>Private Route</p>
    );
}

export default PrivateRoute;